# 11. API Specification

> For the web app. Endpoints below are an initial sketch — expand with request/response schemas, auth, and error handling.

```
POST  /campaigns
POST  /goals/analyze
GET   /channels/recommend
POST  /campaigns/{id}/generate
GET   /plans
PATCH /todos/{id}
```
